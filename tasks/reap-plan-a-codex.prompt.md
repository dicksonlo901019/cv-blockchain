Implement the task in `tasks/reap-plan-a-codex.md`.

Before changing code:

1. Inspect the repository structure and current route/content architecture.
2. Create and switch to branch `feat/reap-plan-a-cv-variants`.
3. Keep the current production CV unchanged.

Complete both tailored CV variants, validate browser/mobile/print output, and open a draft PR to `main`. Do not merge.
